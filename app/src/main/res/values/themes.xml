<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Codex_JAVA" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Base customizations can go here -->
    </style>

    <!-- Morning Theme (Light) -->
    <style name="Theme.Codex.Morning" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/morning_primary</item>
        <item name="colorOnPrimary">@color/morning_onPrimary</item>
        <item name="android:colorBackground">@color/morning_background</item>
        <item name="colorOnBackground">@color/morning_onBackground</item>
        <item name="colorSurface">@color/morning_surface</item>
        <item name="colorOnSurface">@color/morning_onSurface</item>
    </style>

    <!-- Day Theme (Light) -->
    <style name="Theme.Codex.Day" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/day_primary</item>
        <item name="colorOnPrimary">@color/day_onPrimary</item>
        <item name="android:colorBackground">@color/day_background</item>
        <item name="colorOnBackground">@color/day_onBackground</item>
        <item name="colorSurface">@color/day_surface</item>
        <item name="colorOnSurface">@color/day_onSurface</item>
    </style>

    <!-- Evening Theme (Dark) -->
    <style name="Theme.Codex.Evening" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/evening_primary</item>
        <item name="colorOnPrimary">@color/evening_onPrimary</item>
        <item name="android:colorBackground">@color/evening_background</item>
        <item name="colorOnBackground">@color/evening_onBackground</item>
        <item name="colorSurface">@color/evening_surface</item>
        <item name="colorOnSurface">@color/evening_onSurface</item>
    </style>

    <!-- Night Theme (Dark) -->
    <style name="Theme.Codex.Night" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/night_primary</item>
        <item name="colorOnPrimary">@color/night_onPrimary</item>
        <item name="android:colorBackground">@color/night_background</item>
        <item name="colorOnBackground">@color/night_onBackground</item>
        <item name="colorSurface">@color/night_surface</item>
        <item name="colorOnSurface">@color/night_onSurface</item>
    </style>

</resources>
